import React from 'react';
import { ImSpinner } from 'react-icons/im';

const Loading = () => {
  return (
    <div className="loading-section">
      <ImSpinner className="loading" />
    </div>
  );
};

export default Loading;
